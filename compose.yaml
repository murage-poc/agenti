services:
  archgw:
    image: katanemo/archgw:latest
    ports:
      - "8080:8080"
      - "12000:12000"
    volumes:
      - ./arch_config.yaml:/app/arch_config.yaml
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:?error}
    networks:
      - bluebell
  agenti:
    image: docker.io/library/demo
    networks:
      - bluebell

networks:
  bluebell:
